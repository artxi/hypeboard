services:
  - type: web
    name: hypeboard-backend
    runtime: docker
    dockerfilePath: ./packages/backend/Dockerfile
    dockerContext: .
    region: oregon
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
    healthCheckPath: /

  - type: web
    name: hypeboard-frontend
    env: static
    buildCommand: npm install -g pnpm@10.30.0 && HUSKY=0 pnpm install --ignore-scripts && pnpm --filter frontend build
    staticPublishPath: ./packages/frontend/dist
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: HUSKY
        value: 0
      - key: CI
        value: true
